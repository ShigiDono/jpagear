# Compatibility
CMAKE_MINIMUM_REQUIRED (VERSION 2.6)

SET(PRE_ROOT_DIR ${ROOT_DIR})
SET(ROOT_DIR "${PROJECT_SOURCE_DIR}/games")
ADD_SUBDIRECTORY("${ROOT_DIR}/alpha" alpha)



SET(ROOT_DIR ${PRE_ROOT_DIR})